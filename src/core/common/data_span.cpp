#include "data_span.hpp"

#include <stdexcept>

// Implementation file for data_span
// Most functionality is header-only (constexpr), but we can add
// non-constexpr utility functions here if needed

namespace kerntopia {

// Currently no implementation needed as data_span is header-only
// This file exists for potential future expansion and to maintain
// consistent CMake structure

} // namespace kerntopia